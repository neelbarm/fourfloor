# docs

`screenshot.png` is referenced from the README and is not committed yet.

To produce it, run the demo and capture the generated preview page:

```bash
make demo
open examples/preview.html      # screenshot the header + waveform
```

The shot that reads best is the top of the page: the title with BPM and key,
the stat row, and the waveform with the section map coloured by slot.
